<ul id="nav">
	<% nav.each do |item| %>
		<li class="<%= active_page?(item.slug) ? 'current_page_item' : ''%>">
			<%= link_to item.title, content_url(:slug => item.slug) %>
		</li>
	<% end %>
	<li><a href="#">Apply</a></li>
	<li><a href="#">Contact</a></li>
</ul>